import {routes} from '../../routes/paths'
import api from '../../services/api'

const ETokens = {
  title: {
    name: ['Token', 'Tokens'],
  },
  routes: routes.nft,
  api: api.nft,
}

export default ETokens
