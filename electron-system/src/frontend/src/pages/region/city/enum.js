import {routes} from '../../../routes/paths'
import api from '../../../services/api'

const ECity = {
  title: {
    name: ['city', 'cities'],
  },
  // routes: routes.City,
  api: api.city,
}

export default ECity
