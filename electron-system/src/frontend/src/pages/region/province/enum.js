import {routes} from '../../../routes/paths'
import api from '../../../services/api'

const EProvince = {
  title: {
    name: ['province', 'provinces'],
  },
  // routes: routes.province,
  api: api.province,
}

export default EProvince
