import {routes} from '../../../routes/paths'
import api from '../../../services/api'

const EPublicForm = {
  title: '',
  bseName: 'a_',
  routes: routes.form,
  api: api.public.form,
}

export default EPublicForm
